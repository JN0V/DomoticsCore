#pragma once

/**
 * \mainpage DomoticsCore-Wifi
 *
 * WiFi connectivity component for DomoticsCore with optional WebUI provider.
 * Provides non-blocking STA connection management, AP and STA+AP modes, and
 * asynchronous network scanning.
 *
 * \section features Features
 * - STA connection with reconnection
 * - AP and AP+STA modes
 * - Async scanning (non-blocking)
 * - Simple API: enableWifi, enableAP, setCredentials, reconnect
 * - Optional WebUI provider for settings and status
 */

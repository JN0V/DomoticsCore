#pragma once

/**
 * \mainpage DomoticsCore-Storage
 *
 * Key-value storage component for DomoticsCore built on ESP32 Preferences/NVS.
 *
 * \section features Features
 * - Namespaced key-value storage (strings, numbers, booleans)
 * - Caching and periodic maintenance
 * - Simple API for get/set/remove/clear
 * - Optional WebUI provider for basic stats and settings
 */
